sviluppando in django, dato il segufrom django.db import models

class Anagrafica(models.Model):
    nome = models.CharField(max_length=30, blank=True, null=True)
    cognome = models.CharField(max_length=30, blank=True, null=True)

    def __str__(self):
        return str(self.nome) + " " + str(self.cognome)

    class Meta:
        verbose_name = "Anagrafica"
        verbose_name_plural = "Anagrafiche"ente models.py







generami in base al seguente template: mostra_lista.html


{% extends 'base1.html' %}
{% load static %}

<!-- Gestione Imprese -->

{% block lista %}

    

    
{% endblock lista %}




per leggere tutti i dati dell'anagrafica